// TypeScript source mirror for content-hotstar.js. Keep the JS file as the load-unpacked MV3 artifact.
(() => {
  window.WatchPartyOttCore.createAdapter({
    provider: 'hotstar',
  }).start();
})();
